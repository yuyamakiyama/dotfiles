alias c='code .'
alias d='docker exec -it $(docker ps |peco|awk "{print \$1}") bash'
alias dsh='docker exec -it $(docker ps |peco|awk "{print \$1}") sh'
alias g='git'
alias dps='docker ps'
alias dup='docker compose up -d'
alias dst='docker compose stop'
alias ddo='docker compose down'
alias dlo='docker compose logs -f'
alias in='if jq -e ".scripts.preinstall" package.json &>/dev/null; then bun preinstall && bun install; else bun install; fi'
alias hs='bun hasura:sync'
alias hsl='bun hasura:sync:local'
alias hst='bun hasura:sync:test'
alias hc='bun hasura:console'
alias gup='bun update-graphql-schema'
alias ggen='bun graphql-codegen'
alias bld='bun run build'
alias tsc='bun run typecheck'
alias lint='bun run lint'
alias format='bun run format'
alias cl='claude --dangerously-skip-permissions'
alias difit='bunx --bun difit'
alias n='nvim'
alias lg='lazygit'
alias rai='dinii-internal__review-agent-integrator'
